import React from "react ";
import { View, TouchableOpacity, Text } from "react-native";

const createProfile = () => {
  return (
    <View>
      <Text>Create Profile Works</Text>
    </View>
  );
};
export default createProfile;